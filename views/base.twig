{% block html_head_container %}
{% include 'html-header.twig' %}
    {% block head %}
    {% endblock %}
    </head>
{% endblock %}

    <body class="{{body_class}} wf-loading" data-template="base.twig" id="pageTop">
        
        <div class="visually-hidden">{% include 'views/partials/svg-defs.svg' %}</div>

        {% block header %}
            {% include 'site/site-header.twig' %}
        {% endblock %}  
        
        <main class="site__main contain--lg" role="main">
        
            {% block before_content %}
                {% include 'pages/_page_header.twig' %}
            {% endblock %}

            {{sidebar ? '<div class="grid">'}}
                
                {{sidebar ? '<div class="main__content">'}}

                    {% block content %}
                        Sorry, no content
                    {% endblock %}

                    {% block after_content %}{% endblock %}
                {{sidebar ? '</div>'}}

                {% if sidebar %}
                    <aside class="main__sidebar">
                        <ul class="list--widgets">
                            {{sidebar}}
                        </ul>
                    </aside>
                {% endif %}

            {{sidebar ? '</div>'}}

        </main>

        {% block footer %}
            {% include 'site/site-footer.twig' %}   
            {{ function('wp_footer') }}
        {% endblock %}

    </body>
</html>
