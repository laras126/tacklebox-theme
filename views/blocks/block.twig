<div class="block--{{block_class}}{{blck.cta_rotate ? ' rotate-' ~ blck.cta_rotate }}">
	{{ block.contain ? '<div class="' ~ block.contain ~ '"</div>' }}
		{% block content %}
			<p>Nothing in this Block</p>
		{% endblock %}
	{{ block.contain ? '</div>' }}
</div> <!-- /.block--{{block_class}} -->