<nav class="section-nav">

	<ul class="section-nav-items">
		{% for item in post.get_field('sections') %}
			<li class="section-nav-item">
				<a class="section-nav-item-link" href="#{{item.section_menu_item_title|replace({ (' '): '_' })|lower}}">{{item.section_menu_item_title}}</a>
			</li>
		{% endfor %}
	</ul>

</nav> <!-- /.content-nav -->
