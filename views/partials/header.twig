<header class="page-header">
	
	<h1>{{post.post_title}}</h1>
	{% if post.header_tagline %}
		<p class="lead">{{post.header_tagline}}</p>
	{% endif %}
	{% if post.header_summary %}
		<p class="summary">{{post.header_summary|wpautop}}</p>
	{% endif %}
	
</header>